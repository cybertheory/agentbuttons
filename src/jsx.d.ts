/**
 * Aggregated type declarations for all agent button custom elements.
 * Provides IntelliSense for JSX, Vue, Solid, and Svelte.
 */

// Re-export underlying package type augmentations so they merge into
// global interfaces when agentbuttons is the only direct dependency.
// Each package already declares the relevant entries — importing them
// here ensures the augmentations are loaded.

/// <reference types="claudebuttons" />
/// <reference types="hermesbuttons" />
/// <reference types="clawbuttons" />
/// <reference path="./preferred-jsx.d.ts" />

export {};
